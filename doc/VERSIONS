munin-plugins Repository Version History
========================================

v26.08 (2026-08-26)
-------------------
- Add the version history description guidelines to the end of this file.
- Describe the repository structure and documentation roles in the README.
- Establish doc/POLICY.md as the self-contained implementation and maintenance policy for compatibility, safety, change discipline, Munin interfaces, privilege, portability, installation, validation, and versioning.
- Add doc/FEATURES.md as the detailed user-facing reference for plugin behavior, configuration, privilege, installation, and troubleshooting.
- Require systemd_failed autoconf to verify that systemctl can query the system manager.
- Improve installer portability and prerequisite command validation across the installers.

v26.07 (2026-07-31)
-------------------
- Stop calling sudo from process_monitoring and document munin-node's 'user root' setting instead, so the munin account no longer needs blanket sudo access to iptables.
- Add sshd process monitoring to process_monitoring with its own toggle variable, enabled by default.
- Add xrdp process monitoring to process_monitoring with its own toggle variable, disabled by default.
- Move plugins under plugins/ and installers under installer/, resolving the plugin source path relative to each installer.
- Add systemd_failed plugin and its installer to report the number of failed systemd units.
- Replace awk {n,} interval expression in usage() with a portable equivalent for mawk compatibility.
- Apply the same portable awk fix to process_monitoring's usage() function.
- Fix README's Included Plugin, Configuration, and Features sections to reflect per-target toggle variables instead of a symlink-based architecture.
- Remove unused id and rm from check_commands in install_process_monitoring.sh.

v25.10 (2025-10-24)
-------------------
- Standardize all shell scripts by removing trailing exit status lines (exit $?) for POSIX-safe reuse.

v25.09 (2025-09-26)
-------------------
- Add per-target toggle variables to process_monitoring allowing on off control.
- POSIX compliance, standardization, and hardening.

v25.08 (2025-08-01)
-------------------
- Added uninstaller.

v25.03 (2025-03-26)
-------------------
- Completed process_monitoring plugin and its installer.

First Commit (2013-12-12)
-------------------------
- Created repository and initial experiments with simple shell scripts.

Version History Guidelines
==========================
- Write one coherent change per physical line, as a rule.
- Keep entries near 100 columns where practical. Necessary file names,
  command names, option names, and configuration names may extend an entry
  to approximately 120 columns.
- When a version history already exists, balance a new entry against the
  length of the existing lines, so that the whole file stays consistent.
  Holding to the form this file has settled on comes before the one physical
  line above, and leaves that rule standing wherever no such form exists.
- When an entry becomes substantially longer, omit or abstract implementation
  details, examples, reasons, and secondary effects where possible.
- Preserve the changed target, externally observable behavior, compatibility
  or security impact, and important option or configuration names.
- Combine related changes within the same version, especially changes to the
  same file or feature.
- Place closely related entries near each other.
- Append an independent new change to the end of the current version entry.
